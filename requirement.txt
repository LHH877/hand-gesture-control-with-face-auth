firebase-admin==6.4.0
opencv-python==4.9.0.80
face-recognition==1.3.0
numpy==1.24.4
mediapipe==0.10.3
Pillow==10.3.0

# Optional standard libraries (no need to install via pip, but listed for clarity)
# pickle - built-in
# os - built-in
# threading - built-in
# time - built-in
# datetime - built-in
# tkinter - usually comes with Python, for Windows you may need to ensure it's installed

